# Lambda image deps — deliberately NOT pyproject.toml: no torch, no
# sentence-transformers (query embedding uses onnxruntime). pymupdf IS here so
# the /documents/{id}/index endpoint can parse uploaded PDFs in-Lambda; it is
# a small pure-wheel (~20 MB) that keeps the image inside ECR's 500 MB tier.
fastapi~=0.115
mangum~=0.19
pydantic~=2.9
pydantic-settings~=2.6
httpx~=0.27
langgraph~=0.2
faiss-cpu~=1.9
rank-bm25~=0.2.2
numpy~=2.1
onnxruntime~=1.20
tokenizers~=0.21
pymupdf~=1.24
