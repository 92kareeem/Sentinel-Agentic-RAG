# Lambda image deps — deliberately NOT pyproject.toml: no torch, no
# sentence-transformers, no pymupdf (ingestion runs on the laptop; the
# /documents/{id}/index endpoint is a stub until it moves here).
fastapi~=0.115
mangum~=0.19
pydantic~=2.9
pydantic-settings~=2.6
httpx~=0.27
langgraph~=0.2
faiss-cpu~=1.9
rank-bm25~=0.2.2
numpy~=2.1
onnxruntime~=1.20
tokenizers~=0.21
